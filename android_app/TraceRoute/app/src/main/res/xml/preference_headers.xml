<?xml version="1.0" encoding="utf-8"?>
<preference-headers xmlns:android="http://schemas.android.com/apk/res/android">

    <header android:fragment="com.gps.capstone.traceroute.settings.Prefs1Frag"
        android:icon="@drawable/ic_action_settings"
        android:title="Settings"
        android:summary="The user settings and the Open GL settings" />

    <header android:fragment="com.gps.capstone.traceroute.settings.UserSettings$Prefs2Fragment"
        android:icon="@drawable/ic_action_picture"
        android:title="Not Used"
        android:summary="Not sure if this will be helpful since we may not need multiple many fields,
        unless we want to experiment with many different settings">
        <!-- Arbitrary key/value pairs can be included with a header as
             arguments to its fragment. -->
        <extra android:name="someKey" android:value="someHeaderValue" />
    </header>

</preference-headers>